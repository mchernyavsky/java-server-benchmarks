jar {
    from {
        configurations.compile.collect {
            it.isDirectory() ? it : zipTree(it)
        }
    }

    manifest {
        attributes 'Main-Class': 'com.github.mchernyavsky.javaserverbenchmarks.client.ClientMain'
    }
}

dependencies {
    compile project(':commons')
}
